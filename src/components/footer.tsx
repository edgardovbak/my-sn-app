import * as React from 'react';

class Footer extends React.Component {
  public render() {
    return (
        <footer>
            footer block
        </footer>
    );
  }
}

export default Footer;
